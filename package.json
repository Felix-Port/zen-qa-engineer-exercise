{
  "name": "zenith-bank-test",
  "version": "1.0.0",
  "description": "Simple zenith bank application test",
  "main": "index.js",
  "scripts": {
    "clean:report": "rm -rf cypress/reports",
    "cypress:open": "cypress open",
    "test-ci": "npm run clean:report && cypress run --headless --browser chrome"
  },
  "author": "Felix Portuphy",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^13.6.3",
    "cypress-mochawesome-reporter": "^3.2.2",
    "cypress-multi-reporters": "^1.6.1",
    "mocha": "^10.0.0"
  }
}
